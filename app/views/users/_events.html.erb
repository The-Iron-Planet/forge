<!-- this page is done for now -->
<div class="scroll-container">




        <% @events.each do |event| %>
          <div class="event-block">
            <p class="dashboard-event-title">
              <%= event.name%>
            </p>
              <p class="dashboard-event-details">
                <%= event.event_date %>
              </p>
              <p class="dashboard-event-details">
                <%= event.event_time %>
              </p>
              <!-- <p class="link">
                <%= event.website %>
              </p> -->




              <p class="dashboard-event-details">
                <%= event.campus.long_name %>
              </p>

              <!-- <p>
                <%= event.description %>
              </p> -->


            <div class="dashboard-edit-delete">
              <%= link_to '<i class="glyphicon glyphicon-pencil"  title="Event"></i>'.html_safe,
                    edit_event_path(event) if event.user_id == current_user.id %>
              <%= link_to '<i class="glyphicon glyphicon-remove " title="Delete"></i>'.html_safe,
                    event, method: :delete, data: { confirm: 'Are you sure?' } if event.user_id == current_user.id %>
              </div>
                    <!-- <div class="event-images-bk">
                      <div>
                        <% if event.uploaded_file_file_name.nil? %>
                        <div><%= image_tag "event_default.png", size: "100x100", :class=>"default-img" %></div>
                        <% else %>
                        <div>
                          <%= image_tag event.uploaded_file(:thumb) %>
                        </div>
                        <% end %>
                      </div>

                    </div> -->

            <!-- <div class="thumbnail ">
              <% if event.user.uploaded_file_file_name.nil? %>
              <div>
                <%=link_to image_tag("default_pic.png", size: "50x50"), "users/#{event.user_id}" %>
                <%=link_to event.user.first_name, "/users/#{event.user_id}" %>
              </div>
              <% else %>
              <div>
                <%=link_to image_tag(event.user.uploaded_file(:thumb)),"users/#{event.user_id}" %>
                <%=link_to event.user.first_name, "/users/#{event.user_id}" %>
              </div>
            </div>-->

      <!-- <% end %> -->







</div>

  <% end %>
</div>
  <!-- </div>
</div>
</div> -->
