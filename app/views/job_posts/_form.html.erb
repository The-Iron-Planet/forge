<div class="container-fluid">
  <div class="row">
    <div class="col-med-12">
      <%= form_for(@job_post) do |f| %>
        <% if @job_post.errors.any? %>
          <div id="error_explanation">
            <h4 class="form-val"><%= pluralize(@job_post.errors.count, "error") %> prohibited this job_post from being saved:</h2>
            <ul class="error">
            <% @job_post.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>


  <div class="row">
    <div class="col-md-12 field">
      <div class="job-post-list">
        <div class="field">
          <%= f.label :title, :class => "input-label" %><br>
          <%= f.text_field :title, :class => "input-field" %>
        </div>
        <div class="field">
          <%= f.label :description, :class => "input-label" %><br>
          <%= f.text_area :description, :class => "description" %>
        </div>
        <div class="field">
          <%= f.label :company_name, :class => "input-label" %><br>
          <%= f.text_field :company_name, :class => "company_name" %>
        </div>
        <div class="field">
          <%= f.label :city, :class => "input-label" %><br>
          <%= f.text_field :city, :class => "city" %>
        </div>
        <div class="field">
          <%= f.label :state, :class => "input-label" %><br>
          <%= f.select :state, options_for_select(us_states, @job_post.state), :include_blank => true, :class => "input-field" %>
        </div>
        <div class="field">
          <%= f.label :experience_desired, :class => "input-label" %><br>
          Junior <%= f.radio_button :experience_desired, "Junior" %>
          Intermediate <%= f.radio_button :experience_desired, "Intermediate" %>
          Senior <%= f.radio_button :experience_desired, "Senior" %>
        </div>
        <div class="field">
          <%= f.label :curriculum_id, "Which students would be most well-suited for the position?", :class => "input-label" %><br>
          <% Curriculum.all.map do |c| %>
          <%= "#{c.name}" %> <%= f.radio_button :experience_desired, "#{c.id}" %>
          <% end %>
        </div>
        <div class="field">
          <%= f.label :website, "Link to job description", :class => "input-label" %><br>
          <%= f.text_field :website, :class => "input-field" %>
        </div>
        <% if @job_post.expires_on == nil %>
          <div class="field">
            <%= f.label :expires_on, "When should this post expire? (Posts expire one month after creation unless otherwise specified.)", :class => "input-label" %><br>
            <%= f.date_field :expires_on, :value => (Date.today + 1.month) %>
          </div>
        <% elsif @job_post.expires_on < Date.today %>
          <div class="field">
            <%= f.label :expires_on, "This post has expired. Please update the expiration date if you would like to reactivate it.", :class => "input-label" %><br>
            <%= f.date_field :expires_on %>
        <% else %>
          <div class="field">
            <%= f.label :expires_on, "When should this post expire?", :class => "input-label" %><br>
            <%= f.date_field :expires_on %>
          </div>
        <% end %>
        <% if @job_post.active != nil %>
          <div class="field">
            <%= f.label :active, "Is this Job Post active?", :class => "input-label" %><br>
            Yes<%= f.radio_button :active, true %>
            No<%= f.radio_button :active, false %>
          </div>
        <% end %>
      </div>
    </div>
        <div class="actions">
          <%= f.submit :class=> "btn", value: "Save Job Post" %>
        </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
